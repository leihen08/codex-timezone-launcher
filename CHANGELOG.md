# 更新日志

## 1.0.1 - 2026-09-07

### 修复

- Microsoft Store 版不再把受保护的 `WindowsApps` 内部 EXE 当作普通程序直接启动；改用带进程级 `TZ` 的 AUMID 激活与线程恢复握手。
- 增大时区说明文字与分隔线的间距，避免高 DPI 显示下文字被线条遮挡。

### 新增

- 时区组合框支持不区分大小写的关键字搜索和动态过滤。
- Store 启动在后台执行，等待期间界面保持响应。
- 交互式启动器使用系统级命名锁限制为单实例；重复打开时只唤回现有窗口。

## 1.0.0 - 2026-09-07

### 新增

- 原生 Windows IANA 时区选择与记忆。
- 只对子进程生效的安全 Codex/ChatGPT 启动。
- Store、App Paths 和常见目录的客户端自动发现。
- 已运行客户端提示，且永不强制结束进程。
- 固定 HTTPS 公网 IP 时区定位，不保存 IP。
- 旧版设置兼容迁移、自动化测试和可复现 Release 构建。
